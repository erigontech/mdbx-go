name: Test
on: [ push ]
jobs:

  test:
    strategy:
      matrix:
        os: [ windows-latest ] # list of os: https://github.com/actions/virtual-environments
        go: [ '1.16' ]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v2

      - name: checkout submodule
        run: |
          git submodule init
          git submodule update

#      - name: Add msbuild to PATH
#        uses: microsoft/setup-msbuild@v1.0.2

      - uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}

      - uses: actions/cache@v2
        with:
          path: ~/go/pkg/mod
          key: go-${{ matrix.os }}-${{ matrix.go }}-${{ hashFiles('**/go.sum') }}

#      - name: Test1
#        run: ls && cd libmdbx && ls && cmake . && cmake --build . -DCMAKE_C_COMPILER_ID=MINGW64 && ls && ls Debug
#         -DMDBX_BUILD_TYPE=Release
#-DCMAKE_BUILD_TYPE=Release

      - name: Test
        run: make win

#https://github.com/golang/go/issues/20982
